name: IITM Test Workflow

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: 24f1000952@ds.study.iitm.ac.in
        run: echo "Hello, world!"

      - name: Final Step
        run: echo "Workflow Completed!"
